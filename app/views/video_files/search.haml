%h1 Поиск видео
= form_tag search_video_files_path, :method => 'get' do
  %p
    = label_tag :title
    = text_field_tag :title
  %p
    = submit_tag "Поиск"


- if @title
  %table
    - @video_files.each do |file|
      %tr
        %td= link_to file.title, video_file_path(file)